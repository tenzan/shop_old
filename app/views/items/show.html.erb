<%= @item.name %>
<%= @item.quantity %>
<%= @item.unit_price %>
<%= @item.url %>
<%= @item.note %>

<%= link_to 'Edit Item', edit_item_path(@item) %>
<%= link_to 'Delete Item',
            item_path(@item),
            method: :delete,
            data: { confirm: 'Are you sure to delete this item?'}
%>